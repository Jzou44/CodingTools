---
title: "Number to Words Converter — Coding.Tools"
description: "Convert numbers to English words, ordinals, and ordinal numbers instantly."
toolTitle: "Number to Words"
toolDescription: "Convert integers to English words, ordinal words, and ordinal numbers."
category: "string-text-utilities"
categoryName: "String & Text Utilities"
activeTool: "number-to-words"
permalink: "/number-to-words.html"
---

{% extends "tool-layout.njk" %}

{% block head_extra %}
  <link rel="stylesheet" href="/css/tool.css">
  <script src="/js/numberToWords.min.js"></script>
{% endblock %}

{% block tool_content %}
  <!-- Input Section -->
  <label class="section-label" for="input-number">Number</label>
  <div class="input-field-wrapper">
    <input type="text" class="input-field" id="input-number" spellcheck="false" placeholder="Enter a number (e.g. 42, -7, 1000000)" autocomplete="off" />
  </div>

  <!-- Toolbar -->
  <div class="toolbar">
    <div class="toolbar-left">
      <button class="btn btn-primary" id="btn-convert" type="button">
        <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
          <polyline points="17 1 21 5 17 9"></polyline>
          <path d="M3 11V9a4 4 0 0 1 4-4h14"></path>
          <polyline points="7 23 3 19 7 15"></polyline>
          <path d="M21 13v2a4 4 0 0 1-4 4H3"></path>
        </svg>
        Convert
      </button>
      <button class="btn btn-ghost" id="btn-clear" type="button">
        <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
          <polyline points="3 6 5 6 21 6"></polyline>
          <path d="M19 6v14a2 2 0 0 1-2 2H7a2 2 0 0 1-2-2V6m3 0V4a2 2 0 0 1 2-2h4a2 2 0 0 1 2 2v2"></path>
          <line x1="10" y1="11" x2="10" y2="17"></line>
          <line x1="14" y1="11" x2="14" y2="17"></line>
        </svg>
        Clear
      </button>
    </div>
    <div class="toolbar-right">
      <button class="btn btn-ghost" id="btn-copy" type="button">
        <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
          <rect x="9" y="9" width="13" height="13" rx="2" ry="2"></rect>
          <path d="M5 15H4a2 2 0 0 1-2-2V4a2 2 0 0 1 2-2h9a2 2 0 0 1 2 2v1"></path>
        </svg>
        Copy
      </button>
    </div>
  </div>

  <!-- Output Section -->
  <div style="margin-top: 24px;">
    <label class="section-label" for="result-word">Word Result</label>
    <div class="input-field-wrapper">
      <input type="text" class="input-field readonly" id="result-word" readonly placeholder="e.g. forty-two" />
    </div>

    <label class="section-label" for="result-ordinal-word" style="margin-top: 16px;">Ordinal Word Result</label>
    <div class="input-field-wrapper">
      <input type="text" class="input-field readonly" id="result-ordinal-word" readonly placeholder="e.g. forty-second" />
    </div>

    <label class="section-label" for="result-ordinal-number" style="margin-top: 16px;">Ordinal Number Result</label>
    <div class="input-field-wrapper">
      <input type="text" class="input-field readonly" id="result-ordinal-number" readonly placeholder="e.g. 42nd" />
    </div>
  </div>

  <!-- Status Bar -->
  <div class="status-bar">
    <div class="status-message idle" id="status-message">
      <span class="status-dot" id="status-dot" style="display:none;"></span>
      <span id="status-text"></span>
    </div>
    <div class="status-meta">
      <span id="char-count">0 characters</span>
    </div>
  </div>
{% endblock %}

{% block description %}
  <h2>Number to Words Converter</h2>
  <p>This tool converts any integer into its English word representation, ordinal word, and ordinal number format. It is useful for writing checks, generating human-readable reports, creating educational materials, or formatting numbers in legal documents.</p>
  <p>The conversion runs locally in your browser using the number-to-words library. The number you enter is not uploaded, stored, or sent to a server.</p>

  <h3>How to use</h3>
  <ol class="steps-list">
    <li>Type or paste a number into the input field.</li>
    <li>Click <strong>Convert</strong> or simply type to see live results.</li>
    <li>Three results are displayed: the word form (e.g. "forty-two"), the ordinal word (e.g. "forty-second"), and the ordinal number (e.g. "42nd").</li>
    <li>Use <strong>Copy</strong> to copy the word result to the clipboard, or <strong>Clear</strong> to reset.</li>
  </ol>

  <h3>What is number to words conversion?</h3>
  <p>Number to words conversion transforms a numeric value into its spelled-out English form. The ordinal form adds the appropriate suffix (st, nd, rd, th) to describe position or order in a sequence. This is commonly used in financial documents, legal writing, and formal correspondence.</p>

  <h3>Input notes</h3>
  <p>The tool accepts integers, including negative numbers and zero. Very large numbers (billions and above) are supported. Decimal numbers are not supported — only whole integers can be converted to words.</p>

  <h3>Example</h3>
  <p>See how different numbers convert to words:</p>
  <div class="code-block-wrapper">
    <button class="code-block-copy" id="example-copy" type="button">Copy</button>
    <pre class="code-block" id="example-code">--- Number: 42 ---
Word: forty-two
Ordinal Word: forty-second
Ordinal Number: 42nd

--- Number: 1000000 ---
Word: one million
Ordinal Word: one millionth
Ordinal Number: 1000000th

--- Number: -7 ---
Word: negative seven
Ordinal Word: negative seventh
Ordinal Number: -7th</pre>
  </div>
{% endblock %}

{% block tool_script %}
  <script>
    (function () {
      'use strict';

      var inputNumber       = document.getElementById('input-number');
      var resultWord        = document.getElementById('result-word');
      var resultOrdinalWord = document.getElementById('result-ordinal-word');
      var resultOrdinalNum  = document.getElementById('result-ordinal-number');
      var btnConvert        = document.getElementById('btn-convert');
      var btnClear          = document.getElementById('btn-clear');
      var btnCopy           = document.getElementById('btn-copy');
      var statusMessage     = document.getElementById('status-message');
      var statusDot         = document.getElementById('status-dot');
      var statusText        = document.getElementById('status-text');
      var charCount         = document.getElementById('char-count');

      function convert() {
        var raw = inputNumber.value.trim();
        if (!raw) {
          resultWord.value = '';
          resultOrdinalWord.value = '';
          resultOrdinalNum.value = '';
          ToolCommon.updateStatus(statusMessage, statusDot, statusText, 'idle', '');
          charCount.textContent = '0 characters';
          return;
        }

        var num = Number(raw);
        if (isNaN(num) || !isFinite(num) || num !== Math.floor(num)) {
          ToolCommon.updateStatus(statusMessage, statusDot, statusText, 'invalid', 'Please enter a valid integer');
          return;
        }

        try {
          var word = numberToWords.toWords(num);
          var ordinalWord = numberToWords.toWordsOrdinal(num);
          var ordinalNum = numberToWords.toOrdinal(num);

          resultWord.value = word;
          resultOrdinalWord.value = ordinalWord;
          resultOrdinalNum.value = ordinalNum;

          ToolCommon.updateStatus(statusMessage, statusDot, statusText, 'valid', 'Converted successfully');
          charCount.textContent = ToolCommon.pluralise(word.length, 'character');
        } catch (e) {
          ToolCommon.updateStatus(statusMessage, statusDot, statusText, 'invalid', 'Conversion failed: ' + e.message);
        }
      }

      inputNumber.addEventListener('input', convert);

      btnConvert.onclick = convert;

      btnClear.addEventListener('click', function () {
        inputNumber.value = '';
        resultWord.value = '';
        resultOrdinalWord.value = '';
        resultOrdinalNum.value = '';
        ToolCommon.updateStatus(statusMessage, statusDot, statusText, 'idle', '');
        charCount.textContent = '0 characters';
      });

      btnCopy.addEventListener('click', function () {
        if (resultWord.value) {
          navigator.clipboard.writeText(resultWord.value).then(function () {
            var original = btnCopy.textContent;
            btnCopy.textContent = 'Copied!';
            setTimeout(function () { btnCopy.textContent = original; }, 1500);
          });
        }
      });

      ToolCommon.initExampleCopy();
    })();
  </script>
{% endblock %}
