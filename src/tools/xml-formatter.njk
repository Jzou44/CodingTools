---
title: "XML Formatter — Coding.Tools"
description: "Beautify and format XML documents with proper indentation and structure. Runs locally in your browser, fast and free."
toolTitle: "XML Formatter"
toolDescription: "Beautify XML documents with consistent indentation and proper nesting. Copy or download the formatted result without sending data anywhere."
category: "formatter-minifier"
categoryName: "Formatter & Minifier"
activeTool: "xml-formatter"
permalink: "/xml-formatter.html"
---

{% extends "tool-layout.njk" %}

{% block head_extra %}
  <link rel="stylesheet" href="/css/tool.css">
  <script src="/js/vkbeautify.js"></script>
{% endblock %}

{% import "macros/editor.njk" as editor %}
{% import "macros/toolbar.njk" as toolbar %}
{% import "macros/status-bar.njk" as status %}

{% block tool_content %}
  {{ editor.inputEditor("Paste your XML here...") }}

  {% call toolbar.toolbar("Clear", "Copy", "Download") %}
    <button class="btn btn-primary" id="btn-generate" type="button">
      <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M21 2l-2 2m-7.61 7.61a5.5 5.5 0 1 1-7.778 7.778 5.5 5.5 0 0 1 7.777-7.777zm0 0L15.5 7.5m0 0l3 3L22 7l-3-3m-3.5 3.5L19 4"></path></svg>
      Format XML
    </button>
  {% endcall %}

  {{ editor.outputEditor("Formatted XML will appear here...") }}

  {{ status.statusBar("characters", "lines") }}
{% endblock %}

{% block description %}
  <h2>XML Formatter</h2>
  <p>This XML Formatter tool takes raw or minified XML and reformats it with proper indentation, line breaks, and nesting structure. It makes dense XML documents readable by organizing elements into a clear hierarchy.</p>
  <p>The formatting runs locally in your browser using the vkbeautify library. Your XML data is never uploaded to a server, making it safe for formatting configuration files, SOAP responses, sitemaps, or any XML content.</p>

  <h3>How to use</h3>
  <ol class="steps-list">
    <li>Paste your raw or minified XML into the input editor.</li>
    <li>Click <strong>Format XML</strong> to apply indentation and structure.</li>
    <li>View the beautified XML in the output editor with proper nesting.</li>
    <li>Use <strong>Copy</strong> for the clipboard, <strong>Download</strong> for a .xml file, or <strong>Clear</strong> to reset both editors.</li>
  </ol>

  <h3>What is XML?</h3>
  <p>XML (eXtensible Markup Language) is a markup language designed to store and transport data. It uses tags to define elements and attributes to provide metadata, creating a tree-like structure that is both human-readable and machine-parseable.</p>
  <p>XML is widely used in web services (SOAP, RSS), document formats (DOCX, SVG), configuration files (Maven, Android manifests), and data exchange between heterogeneous systems. Proper formatting is essential for debugging and maintaining large XML documents.</p>

  <h3>Input notes</h3>
  <p>The tool accepts any well-formed XML input. It normalizes whitespace between tags while preserving content within text nodes. The XML declaration and comments are preserved in the formatted output.</p>

  <h3>Example</h3>
  <p>Minified XML becomes readable after formatting:</p>
  <div class="code-block-wrapper">
    <button class="code-block-copy" id="example-copy" type="button">Copy</button>
    <pre class="code-block" id="example-code">--- Input (minified) ---
&lt;root&gt;&lt;user id="1"&gt;&lt;name&gt;Alice&lt;/name&gt;&lt;email&gt;alice@example.com&lt;/email&gt;&lt;/user&gt;&lt;/root&gt;

--- Output (formatted) ---
&lt;root&gt;
  &lt;user id="1"&gt;
    &lt;name&gt;Alice&lt;/name&gt;
    &lt;email&gt;alice@example.com&lt;/email&gt;
  &lt;/user&gt;
&lt;/root&gt;</pre>
  </div>
{% endblock %}

{% block tool_script %}
  <script>
    (function () {
      'use strict';

      var inputEditor    = document.getElementById('input-editor');
      var outputEditor   = document.getElementById('output-editor');
      var btnGenerate    = document.getElementById('btn-generate');
      var btnClear       = document.getElementById('btn-clear');
      var btnCopy        = document.getElementById('btn-copy');
      var btnDownload    = document.getElementById('btn-download');
      var statusMessage  = document.getElementById('status-message');
      var statusDot      = document.getElementById('status-dot');
      var statusText     = document.getElementById('status-text');
      var charCount      = document.getElementById('char-count');
      var lineCount      = document.getElementById('line-count');
      var inputLineNums  = document.getElementById('input-line-numbers');
      var outputLineNums = document.getElementById('output-line-numbers');

      function updateCounts() {
        var text = outputEditor.value;
        charCount.textContent = ToolCommon.pluralise(text.length, 'character');
        lineCount.textContent = ToolCommon.pluralise(ToolCommon.countLines(text), 'line');
        ToolCommon.updateLineNumbers(outputEditor, outputLineNums);
      }

      btnGenerate.onclick = function () {
        var text = inputEditor.value;
        if (!text) return;
        try {
          var minified = htmlminifier.minify(text, {collapseWhitespace: true, minifyJS: true, minifyCSS: true, removeComments: true});
          outputEditor.value = vkbeautify.xml(minified);
          ToolCommon.updateStatus(statusMessage, statusDot, statusText, 'valid', 'XML formatted successfully');
          updateCounts();
        } catch (e) {
          ToolCommon.updateStatus(statusMessage, statusDot, statusText, 'invalid', 'Formatting failed: ' + e.message);
        }
      };

      btnClear.addEventListener('click', function () {
        ToolCommon.clearEditors(inputEditor, outputEditor, statusMessage, statusDot, statusText, charCount, lineCount, inputLineNums, outputLineNums);
      });

      btnCopy.addEventListener('click', function () {
        ToolCommon.copyOutput(outputEditor, btnCopy);
      });

      btnDownload.addEventListener('click', function () {
        ToolCommon.downloadOutput(outputEditor, 'result.xml');
      });

      // Init shared behaviors
      ToolCommon.initInputLineNumbers(inputEditor, inputLineNums);
      ToolCommon.initScrollSync(inputEditor, inputLineNums);
      ToolCommon.initScrollSync(outputEditor, outputLineNums);
      ToolCommon.initTabKey(inputEditor, inputLineNums);
      ToolCommon.initExampleCopy();
      ToolCommon.initKeyboardShortcut(btnGenerate);
      ToolCommon.updateLineNumbers(inputEditor, inputLineNums);
      ToolCommon.updateLineNumbers(outputEditor, outputLineNums);
    })();
  </script>
{% endblock %}
