---
title: "CSS Beautifier — Coding.Tools"
description: "Beautify and format CSS code with proper indentation and structure. Runs locally in your browser, fast and free."
toolTitle: "CSS Beautifier"
toolDescription: "Beautify CSS code with consistent indentation and proper formatting. Copy or download the result without sending data anywhere."
category: "formatter-minifier"
categoryName: "Formatter & Minifier"
activeTool: "css-beautifier"
permalink: "/css-beautifier.html"
---

{% extends "tool-layout.njk" %}

{% block head_extra %}
  <link rel="stylesheet" href="/css/tool.css">
  <script src="https://cdn.jsdelivr.net/npm/js-beautify@1.15.1/js/lib/beautify-css.js"></script>
{% endblock %}

{% block tool_content %}
  <!-- Input Section -->
  <label class="section-label" for="input-editor">Input</label>
  <div class="editor-wrapper">
    <div class="line-numbers" id="input-line-numbers">1</div>
    <textarea class="editor-area" id="input-editor" spellcheck="false" placeholder="Paste your CSS here..."></textarea>
  </div>

  <!-- Toolbar -->
  <div class="toolbar">
    <div class="toolbar-left">
      <button class="btn btn-primary" id="btn-generate" type="button">
        <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M21 2l-2 2m-7.61 7.61a5.5 5.5 0 1 1-7.778 7.778 5.5 5.5 0 0 1 7.777-7.777zm0 0L15.5 7.5m0 0l3 3L22 7l-3-3m-3.5 3.5L19 4"></path></svg>
        Beautify CSS
      </button>
      <button class="btn btn-ghost" id="btn-clear" type="button">
        <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
          <polyline points="3 6 5 6 21 6"></polyline>
          <path d="M19 6v14a2 2 0 0 1-2 2H7a2 2 0 0 1-2-2V6m3 0V4a2 2 0 0 1 2-2h4a2 2 0 0 1 2 2v2"></path>
          <line x1="10" y1="11" x2="10" y2="17"></line>
          <line x1="14" y1="11" x2="14" y2="17"></line>
        </svg>
        Clear
      </button>
    </div>
    <div class="toolbar-right">
      <button class="btn btn-ghost" id="btn-copy" type="button">
        <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
          <rect x="9" y="9" width="13" height="13" rx="2" ry="2"></rect>
          <path d="M5 15H4a2 2 0 0 1-2-2V4a2 2 0 0 1 2-2h9a2 2 0 0 1 2 2v1"></path>
        </svg>
        Copy
      </button>
      <button class="btn btn-ghost" id="btn-download" type="button">
        <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
          <path d="M21 15v4a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-4"></path>
          <polyline points="7 10 12 15 17 10"></polyline>
          <line x1="12" y1="15" x2="12" y2="3"></line>
        </svg>
        Download
      </button>
    </div>
  </div>

  <!-- Output Section -->
  <div style="margin-top: 24px;">
    <label class="section-label" for="output-editor">Output</label>
    <div class="editor-wrapper readonly-wrapper">
      <div class="line-numbers readonly-bg" id="output-line-numbers">1</div>
      <textarea class="editor-area readonly" id="output-editor" spellcheck="false" readonly placeholder="Beautified CSS will appear here..."></textarea>
    </div>
  </div>

  <!-- Status Bar -->
  <div class="status-bar">
    <div class="status-message idle" id="status-message">
      <span class="status-dot" id="status-dot" style="display:none;"></span>
      <span id="status-text"></span>
    </div>
    <div class="status-meta">
      <span id="char-count">0 characters</span>
      <span id="line-count">0 lines</span>
    </div>
  </div>
{% endblock %}

{% block description %}
  <h2>CSS Beautifier</h2>
  <p>This CSS Beautifier takes minified, compressed, or poorly indented CSS and reformats it with consistent indentation, proper spacing, and clean line breaks. It makes dense stylesheets readable and easier to maintain.</p>
  <p>The beautification runs entirely in your browser using the js-beautify library. Your CSS code never leaves your device, making it ideal for formatting minified stylesheets, inspecting framework CSS, or cleaning up generated styles.</p>

  <h3>How to use</h3>
  <ol class="steps-list">
    <li>Paste your minified or messy CSS into the input editor.</li>
    <li>Click <strong>Beautify CSS</strong> to apply indentation and formatting.</li>
    <li>View the formatted CSS in the output editor with proper structure.</li>
    <li>Use <strong>Copy</strong> for the clipboard, <strong>Download</strong> for a .css file, or <strong>Clear</strong> to reset both editors.</li>
  </ol>

  <h3>What is CSS beautification?</h3>
  <p>CSS beautification (also called prettifying or formatting) is the process of reformatting Cascading Style Sheets to follow consistent indentation and spacing rules. It places each property on its own line, adds proper indentation for nested selectors, and ensures braces and semicolons are clearly visible.</p>
  <p>Well-formatted CSS is essential for debugging layout issues, understanding specificity conflicts, and maintaining design systems. Beautified CSS makes it easy to scan selectors, spot duplicate rules, and trace the cascade order.</p>

  <h3>Input notes</h3>
  <p>The tool accepts any valid CSS input, including modern features like CSS variables, grid and flexbox layouts, media queries, and keyframe animations. It handles nested selectors, pseudo-classes, and vendor prefixes correctly.</p>

  <h3>Example</h3>
  <p>Minified CSS becomes readable after beautification:</p>
  <div class="code-block-wrapper">
    <button class="code-block-copy" id="example-copy" type="button">Copy</button>
    <pre class="code-block" id="example-code">--- Input (minified) ---
.card{background:#fff;border:1px solid #ddd;padding:16px;border-radius:8px}.card h2{font-size:1.5rem;margin-bottom:8px}

--- Output (beautified) ---
.card {
  background: #fff;
  border: 1px solid #ddd;
  padding: 16px;
  border-radius: 8px;
}

.card h2 {
  font-size: 1.5rem;
  margin-bottom: 8px;
}</pre>
  </div>
{% endblock %}

{% block tool_script %}
  <script>
    (function () {
      'use strict';

      var inputEditor    = document.getElementById('input-editor');
      var outputEditor   = document.getElementById('output-editor');
      var btnGenerate    = document.getElementById('btn-generate');
      var btnClear       = document.getElementById('btn-clear');
      var btnCopy        = document.getElementById('btn-copy');
      var btnDownload    = document.getElementById('btn-download');
      var statusMessage  = document.getElementById('status-message');
      var statusDot      = document.getElementById('status-dot');
      var statusText     = document.getElementById('status-text');
      var charCount      = document.getElementById('char-count');
      var lineCount      = document.getElementById('line-count');
      var inputLineNums  = document.getElementById('input-line-numbers');
      var outputLineNums = document.getElementById('output-line-numbers');

      function updateCounts() {
        var text = outputEditor.value;
        charCount.textContent = ToolCommon.pluralise(text.length, 'character');
        lineCount.textContent = ToolCommon.pluralise(ToolCommon.countLines(text), 'line');
        ToolCommon.updateLineNumbers(outputEditor, outputLineNums);
      }

      btnGenerate.onclick = function () {
        var text = inputEditor.value;
        if (!text) return;
        try {
          outputEditor.value = css_beautify(text, { indent_size: 2 });
          ToolCommon.updateStatus(statusMessage, statusDot, statusText, 'valid', 'CSS beautified successfully');
          updateCounts();
        } catch (e) {
          ToolCommon.updateStatus(statusMessage, statusDot, statusText, 'invalid', 'Beautification failed: ' + e.message);
        }
      };

      btnClear.addEventListener('click', function () {
        ToolCommon.clearEditors(inputEditor, outputEditor, statusMessage, statusDot, statusText, charCount, lineCount, inputLineNums, outputLineNums);
      });

      btnCopy.addEventListener('click', function () {
        ToolCommon.copyOutput(outputEditor, btnCopy);
      });

      btnDownload.addEventListener('click', function () {
        ToolCommon.downloadOutput(outputEditor, 'result.css');
      });

      // Init shared behaviors
      ToolCommon.initInputLineNumbers(inputEditor, inputLineNums);
      ToolCommon.initScrollSync(inputEditor, inputLineNums);
      ToolCommon.initScrollSync(outputEditor, outputLineNums);
      ToolCommon.initTabKey(inputEditor, inputLineNums);
      ToolCommon.initExampleCopy();
      ToolCommon.initKeyboardShortcut(btnGenerate);
      ToolCommon.updateLineNumbers(inputEditor, inputLineNums);
      ToolCommon.updateLineNumbers(outputEditor, outputLineNums);
    })();
  </script>
{% endblock %}
