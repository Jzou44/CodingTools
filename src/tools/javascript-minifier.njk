---
title: "JavaScript Minifier — Coding.Tools"
description: "Minify and compress JavaScript code by removing whitespace and comments. Runs locally in your browser, fast and free."
toolTitle: "JavaScript Minifier"
toolDescription: "Minify JavaScript by stripping whitespace, comments, and unnecessary characters. Copy or download the result without sending data anywhere."
category: "formatter-minifier"
categoryName: "Formatter & Minifier"
activeTool: "javascript-minifier"
permalink: "/javascript-minifier.html"
---

{% extends "tool-layout.njk" %}

{% block head_extra %}
  <link rel="stylesheet" href="/css/tool.css">
  <script src="https://cdn.jsdelivr.net/npm/html-minifier@4.0.0/dist/htmlminifier.min.js"></script>
{% endblock %}

{% import "macros/editor.njk" as editor %}
{% import "macros/toolbar.njk" as toolbar %}
{% import "macros/status-bar.njk" as status %}

{% block tool_content %}
  {{ editor.inputEditor("Paste your JavaScript here...") }}

  {% call toolbar.toolbar("Clear", "Copy", "Download") %}
    <button class="btn btn-primary" id="btn-generate" type="button">
      <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M21 2l-2 2m-7.61 7.61a5.5 5.5 0 1 1-7.778 7.778 5.5 5.5 0 0 1 7.777-7.777zm0 0L15.5 7.5m0 0l3 3L22 7l-3-3m-3.5 3.5L19 4"></path></svg>
      Minify JavaScript
    </button>
  {% endcall %}

  {{ editor.outputEditor("Minified JavaScript will appear here...") }}

  {{ status.statusBar("characters", "lines") }}
{% endblock %}

{% block description %}
  <h2>JavaScript Minifier</h2>
  <p>This JavaScript Minifier removes unnecessary whitespace, comments, and line breaks from JavaScript code to produce the most compact representation. It reduces file size for faster page loads and lower bandwidth consumption.</p>
  <p>The minification runs locally in your browser using the html-minifier library. Your code never leaves your device, making it safe for processing proprietary scripts, inline JavaScript, or any code that may contain sensitive logic.</p>

  <h3>How to use</h3>
  <ol class="steps-list">
    <li>Paste your formatted JavaScript into the input editor.</li>
    <li>Click <strong>Minify JavaScript</strong> to strip whitespace and comments.</li>
    <li>View the compact code in the output editor.</li>
    <li>Use <strong>Copy</strong> for the clipboard, <strong>Download</strong> for a .js file, or <strong>Clear</strong> to reset both editors.</li>
  </ol>

  <h3>What is JavaScript minification?</h3>
  <p>JavaScript minification is the process of removing all characters from source code that are not required for correct execution. This includes stripping whitespace, removing comments, collapsing line breaks, and shortening variable names in advanced minifiers.</p>
  <p>Minified JavaScript is standard practice in web development. Build tools like Webpack, Rollup, and esbuild include minification as a production step. This browser-based tool provides a quick way to minify individual scripts without a build pipeline.</p>

  <h3>Input notes</h3>
  <p>The tool accepts any JavaScript code. It wraps the input in script tags to leverage the html-minifier library's JS minification capabilities, then strips the wrapper tags from the output. Single-line and multi-line comments are removed.</p>

  <h3>Example</h3>
  <p>Formatted JavaScript is compressed by removing whitespace and comments:</p>
  <div class="code-block-wrapper">
    <button class="code-block-copy" id="example-copy" type="button">Copy</button>
    <pre class="code-block" id="example-code">--- Input (formatted) ---
// Greeting function
function greet(name) {
  if (name) {
    console.log("Hello, " + name + "!");
  } else {
    console.log("Hello!");
  }
}

--- Output (minified) ---
function greet(name){if(name){console.log("Hello, "+name+"!")}else{console.log("Hello!")}}</pre>
  </div>
{% endblock %}

{% block tool_script %}
  <script>
    (function () {
      'use strict';

      var inputEditor    = document.getElementById('input-editor');
      var outputEditor   = document.getElementById('output-editor');
      var btnGenerate    = document.getElementById('btn-generate');
      var btnClear       = document.getElementById('btn-clear');
      var btnCopy        = document.getElementById('btn-copy');
      var btnDownload    = document.getElementById('btn-download');
      var statusMessage  = document.getElementById('status-message');
      var statusDot      = document.getElementById('status-dot');
      var statusText     = document.getElementById('status-text');
      var charCount      = document.getElementById('char-count');
      var lineCount      = document.getElementById('line-count');
      var inputLineNums  = document.getElementById('input-line-numbers');
      var outputLineNums = document.getElementById('output-line-numbers');

      function updateCounts() {
        var text = outputEditor.value;
        charCount.textContent = ToolCommon.pluralise(text.length, 'character');
        lineCount.textContent = ToolCommon.pluralise(ToolCommon.countLines(text), 'line');
        ToolCommon.updateLineNumbers(outputEditor, outputLineNums);
      }

      btnGenerate.onclick = function () {
        var text = inputEditor.value;
        if (!text) return;
        try {
          var wrapped = '<script>' + text + '<\/script>';
          var minified = htmlminifier.minify(wrapped, {collapseWhitespace: true, minifyJS: true, removeComments: true});
          outputEditor.value = minified.replace(/<\/?script>/g, '');
          ToolCommon.updateStatus(statusMessage, statusDot, statusText, 'valid', 'JavaScript minified successfully');
          updateCounts();
        } catch (e) {
          ToolCommon.updateStatus(statusMessage, statusDot, statusText, 'invalid', 'Minification failed: ' + e.message);
        }
      };

      btnClear.addEventListener('click', function () {
        ToolCommon.clearEditors(inputEditor, outputEditor, statusMessage, statusDot, statusText, charCount, lineCount, inputLineNums, outputLineNums);
      });

      btnCopy.addEventListener('click', function () {
        ToolCommon.copyOutput(outputEditor, btnCopy);
      });

      btnDownload.addEventListener('click', function () {
        ToolCommon.downloadOutput(outputEditor, 'result.js');
      });

      // Init shared behaviors
      ToolCommon.initInputLineNumbers(inputEditor, inputLineNums);
      ToolCommon.initScrollSync(inputEditor, inputLineNums);
      ToolCommon.initScrollSync(outputEditor, outputLineNums);
      ToolCommon.initTabKey(inputEditor, inputLineNums);
      ToolCommon.initExampleCopy();
      ToolCommon.initKeyboardShortcut(btnGenerate);
      ToolCommon.updateLineNumbers(inputEditor, inputLineNums);
      ToolCommon.updateLineNumbers(outputEditor, outputLineNums);
    })();
  </script>
{% endblock %}
