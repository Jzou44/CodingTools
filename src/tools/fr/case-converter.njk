---
lang: "fr"
toolId: "case-converter"
category: "string-text-utilities"
activeTool: "case-converter"
permalink: "/fr/case-converter.html"
---

{% extends "tool-layout.njk" %}

{% import "macros/editor.njk" as editor %}
{% import "macros/toolbar.njk" as toolbar %}
{% import "macros/status-bar.njk" as status %}

{% block tool_content %}
  {{ editor.inputEditor(toolData.inputPlaceholder, t.ui.input) }}

  {% call toolbar.toolbar(t.ui.clear, t.ui.copy, t.ui.download) %}
    <button class="btn btn-primary" id="btn-upper" type="button">{{ toolData.upperCase }}</button>
    <button class="btn btn-primary" id="btn-lower" type="button">{{ toolData.lowerCase }}</button>
    <button class="btn btn-primary" id="btn-sentence" type="button">{{ toolData.sentenceCase }}</button>
    <button class="btn btn-primary" id="btn-title" type="button">{{ toolData.titleCase }}</button>
    <button class="btn btn-primary" id="btn-invert" type="button">{{ toolData.invertCase }}</button>
  {% endcall %}

  {{ status.statusBar(t.ui.characters, t.ui.lines) }}
{% endblock %}

{% block description %}
  <h2>{{ toolData.descTitle }}</h2>
  {% for para in toolData.descParas %}
  <p>{{ para | safe }}</p>
  {% endfor %}
  <h3>{{ toolData.howToUse }}</h3>
  <ol class="steps-list">
    {% for step in toolData.steps %}
    <li>{{ step | safe }}</li>
    {% endfor %}
  </ol>
  <h3>{{ toolData.whatIs }}</h3>
  {% for para in toolData.whatIsParas %}
  <p>{{ para | safe }}</p>
  {% endfor %}
  <h3>{{ toolData.inputNotes }}</h3>
  <p>{{ toolData.inputNotesPara | safe }}</p>
  <h3>{{ toolData.example }}</h3>
  <p>{{ toolData.examplePara | safe }}</p>
  <div class="code-block-wrapper">
    <button class="code-block-copy" id="example-copy" type="button">{{ t.ui.copyExample }}</button>
    <pre class="code-block" id="example-code">{{ toolData.exampleCode | safe }}</pre>
  </div>
{% endblock %}

{% block tool_script %}
  <script>
    (function () {
      'use strict';

      var inputEditor   = document.getElementById('input-editor');
      var btnUpper      = document.getElementById('btn-upper');
      var btnLower      = document.getElementById('btn-lower');
      var btnSentence   = document.getElementById('btn-sentence');
      var btnTitle      = document.getElementById('btn-title');
      var btnInvert     = document.getElementById('btn-invert');
      var btnClear      = document.getElementById('btn-clear');
      var btnCopy       = document.getElementById('btn-copy');
      var btnDownload   = document.getElementById('btn-download');
      var statusMessage = document.getElementById('status-message');
      var statusDot     = document.getElementById('status-dot');
      var statusText    = document.getElementById('status-text');
      var charCount     = document.getElementById('char-count');
      var lineCount     = document.getElementById('line-count');
      var inputLineNums = document.getElementById('input-line-numbers');

      function updateCounts() {
        var text = inputEditor.value;
        charCount.textContent = ToolCommon.pluralise(text.length, 'character');
        lineCount.textContent = ToolCommon.pluralise(ToolCommon.countLines(text), 'line');
        ToolCommon.updateLineNumbers(inputEditor, inputLineNums);
      }

      function toSentenceCase(str) {
        return str.toLowerCase().replace(/(^\s*\w|[.!?]\s*\w)/g, function (c) {
          return c.toUpperCase();
        });
      }

      function toTitleCase(str) {
        return str.toLowerCase().replace(/\b\w/g, function (c) {
          return c.toUpperCase();
        });
      }

      function invertCase(str) {
        return str.split('').map(function (c) {
          if (c === c.toUpperCase()) return c.toLowerCase();
          return c.toUpperCase();
        }).join('');
      }

      btnUpper.onclick = function () {
        if (!inputEditor.value) return;
        inputEditor.value = inputEditor.value.toUpperCase();
        ToolCommon.updateStatus(statusMessage, statusDot, statusText, 'valid', '{{ toolData.upperCase }}');
        updateCounts();
      };

      btnLower.onclick = function () {
        if (!inputEditor.value) return;
        inputEditor.value = inputEditor.value.toLowerCase();
        ToolCommon.updateStatus(statusMessage, statusDot, statusText, 'valid', '{{ toolData.lowerCase }}');
        updateCounts();
      };

      btnSentence.onclick = function () {
        if (!inputEditor.value) return;
        inputEditor.value = toSentenceCase(inputEditor.value);
        ToolCommon.updateStatus(statusMessage, statusDot, statusText, 'valid', '{{ toolData.sentenceCase }}');
        updateCounts();
      };

      btnTitle.onclick = function () {
        if (!inputEditor.value) return;
        inputEditor.value = toTitleCase(inputEditor.value);
        ToolCommon.updateStatus(statusMessage, statusDot, statusText, 'valid', '{{ toolData.titleCase }}');
        updateCounts();
      };

      btnInvert.onclick = function () {
        if (!inputEditor.value) return;
        inputEditor.value = invertCase(inputEditor.value);
        ToolCommon.updateStatus(statusMessage, statusDot, statusText, 'valid', '{{ toolData.invertCase }}');
        updateCounts();
      };

      btnClear.addEventListener('click', function () {
        inputEditor.value = '';
        ToolCommon.updateStatus(statusMessage, statusDot, statusText, 'idle', '');
        charCount.textContent = '0 {{ t.ui.characters }}';
        lineCount.textContent = '0 {{ t.ui.lines }}';
        ToolCommon.updateLineNumbers(inputEditor, inputLineNums);
      });

      btnCopy.addEventListener('click', function () {
        ToolCommon.copyOutput(inputEditor, btnCopy);
      });

      btnDownload.addEventListener('click', function () {
        ToolCommon.downloadOutput(inputEditor, 'case-converted.txt');
      });

      ToolCommon.initInputLineNumbers(inputEditor, inputLineNums);
      ToolCommon.initScrollSync(inputEditor, inputLineNums);
      ToolCommon.initTabKey(inputEditor, inputLineNums);
      ToolCommon.initExampleCopy();
      ToolCommon.updateLineNumbers(inputEditor, inputLineNums);

      inputEditor.addEventListener('input', updateCounts);
    })();
  </script>
{% endblock %}
