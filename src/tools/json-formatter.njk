---
title: "JSON Formatter — Coding.Tools"
description: "Beautify and format JSON data with proper indentation and structure. Runs locally in your browser, fast and free."
toolTitle: "JSON Formatter"
toolDescription: "Beautify and format JSON data with proper indentation. Copy or download the formatted result without sending data anywhere."
category: "formatter-minifier"
categoryName: "Formatter & Minifier"
activeTool: "json-formatter"
permalink: "/json-formatter.html"
---

{% extends "tool-layout.njk" %}

{% block head_extra %}
  <link rel="stylesheet" href="/css/tool.css">
  <script src="https://cdn.jsdelivr.net/npm/js-beautify@1.15.4/js/lib/beautify.js"></script>
{% endblock %}

{% import "macros/editor.njk" as editor %}
{% import "macros/toolbar.njk" as toolbar %}
{% import "macros/status-bar.njk" as status %}

{% block tool_content %}
  {{ editor.inputEditor("Paste your JSON here...") }}

  {% call toolbar.toolbar("Clear", "Copy", "Download") %}
    <button class="btn btn-primary" id="btn-generate" type="button">
      <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M21 2l-2 2m-7.61 7.61a5.5 5.5 0 1 1-7.778 7.778 5.5 5.5 0 0 1 7.777-7.777zm0 0L15.5 7.5m0 0l3 3L22 7l-3-3m-3.5 3.5L19 4"></path></svg>
      Format JSON
    </button>
  {% endcall %}

  {{ editor.outputEditor("Formatted JSON will appear here...") }}

  {{ status.statusBar("characters", "lines") }}
{% endblock %}

{% block description %}
  <h2>JSON Formatter</h2>
  <p>This JSON Formatter tool takes raw, minified, or poorly indented JSON and reformats it with consistent indentation and proper structure. It makes dense JSON readable by adding line breaks and spaces according to standard formatting conventions.</p>
  <p>The formatting runs entirely in your browser using the js-beautify library. Your JSON data never leaves your device, making this tool ideal for quickly inspecting API responses, configuration files, or any JSON payload.</p>

  <h3>How to use</h3>
  <ol class="steps-list">
    <li>Paste your raw or minified JSON into the input editor.</li>
    <li>Click <strong>Format JSON</strong> to apply indentation and structure.</li>
    <li>View the beautified JSON in the output editor with proper formatting.</li>
    <li>Use <strong>Copy</strong> for the clipboard, <strong>Download</strong> for a .json file, or <strong>Clear</strong> to reset both editors.</li>
  </ol>

  <h3>What is JSON?</h3>
  <p>JSON (JavaScript Object Notation) is a lightweight data-interchange format that is easy for humans to read and write and easy for machines to parse and generate. It is based on a subset of JavaScript and uses key-value pairs and ordered lists to represent structured data.</p>
  <p>JSON has become the de facto standard for data exchange on the web, used extensively in REST APIs, configuration files, and document databases. Well-formatted JSON is essential for debugging, code reviews, and maintaining readable configuration files.</p>

  <h3>Input notes</h3>
  <p>The tool accepts any valid JSON input, whether it is a single line of minified data or already partially formatted. If the input contains invalid JSON syntax, the tool will report an error so you can fix the structure before formatting.</p>

  <h3>Example</h3>
  <p>Minified JSON becomes readable after formatting:</p>
  <div class="code-block-wrapper">
    <button class="code-block-copy" id="example-copy" type="button">Copy</button>
    <pre class="code-block" id="example-code">--- Input (minified) ---
{"name":"Alice","age":30,"skills":["JavaScript","Python","Go"]}

--- Output (formatted) ---
{
  "name": "Alice",
  "age": 30,
  "skills": [
    "JavaScript",
    "Python",
    "Go"
  ]
}</pre>
  </div>
{% endblock %}

{% block tool_script %}
  <script>
    (function () {
      'use strict';

      var inputEditor    = document.getElementById('input-editor');
      var outputEditor   = document.getElementById('output-editor');
      var btnGenerate    = document.getElementById('btn-generate');
      var btnClear       = document.getElementById('btn-clear');
      var btnCopy        = document.getElementById('btn-copy');
      var btnDownload    = document.getElementById('btn-download');
      var statusMessage  = document.getElementById('status-message');
      var statusDot      = document.getElementById('status-dot');
      var statusText     = document.getElementById('status-text');
      var charCount      = document.getElementById('char-count');
      var lineCount      = document.getElementById('line-count');
      var inputLineNums  = document.getElementById('input-line-numbers');
      var outputLineNums = document.getElementById('output-line-numbers');

      function updateCounts() {
        var text = outputEditor.value;
        charCount.textContent = ToolCommon.pluralise(text.length, 'character');
        lineCount.textContent = ToolCommon.pluralise(ToolCommon.countLines(text), 'line');
        ToolCommon.updateLineNumbers(outputEditor, outputLineNums);
      }

      btnGenerate.onclick = function () {
        var text = inputEditor.value;
        if (!text) return;
        try {
          outputEditor.value = js_beautify(text, { indent_size: 2 });
          ToolCommon.updateStatus(statusMessage, statusDot, statusText, 'valid', 'JSON formatted successfully');
          updateCounts();
        } catch (e) {
          ToolCommon.updateStatus(statusMessage, statusDot, statusText, 'invalid', 'Formatting failed: ' + e.message);
        }
      };

      btnClear.addEventListener('click', function () {
        ToolCommon.clearEditors(inputEditor, outputEditor, statusMessage, statusDot, statusText, charCount, lineCount, inputLineNums, outputLineNums);
      });

      btnCopy.addEventListener('click', function () {
        ToolCommon.copyOutput(outputEditor, btnCopy);
      });

      btnDownload.addEventListener('click', function () {
        ToolCommon.downloadOutput(outputEditor, 'result.json');
      });

      // Init shared behaviors
      ToolCommon.initInputLineNumbers(inputEditor, inputLineNums);
      ToolCommon.initScrollSync(inputEditor, inputLineNums);
      ToolCommon.initScrollSync(outputEditor, outputLineNums);
      ToolCommon.initTabKey(inputEditor, inputLineNums);
      ToolCommon.initExampleCopy();
      ToolCommon.initKeyboardShortcut(btnGenerate);
      ToolCommon.updateLineNumbers(inputEditor, inputLineNums);
      ToolCommon.updateLineNumbers(outputEditor, outputLineNums);
    })();
  </script>
{% endblock %}
